# ---------------------------------------------------------------------------
# Neon Postgres — holds tagged_wallets (~24k rows) and coin_stats.
# Use the POOLED connection string; the app talks to Neon over HTTP, so it
# works on serverless where a raw TCP connection to 5432 would not.
# ---------------------------------------------------------------------------
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DB?sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@HOST.REGION.aws.neon.tech/DB?sslmode=require"

# ---------------------------------------------------------------------------
# Helius — RPC + Enhanced Transactions API. Free tier works but rate-limits on
# busy pools; a paid key makes the analysis noticeably faster.
# ---------------------------------------------------------------------------
HELIUS_API_KEY="your-helius-api-key"
HELIUS_RPC_URL="https://mainnet.helius-rpc.com"

# Historical holder-reconstruction window for the /dashboard view.
HISTORICAL_WINDOW_MINUTES="15"
